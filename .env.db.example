# Database service environment variables.
# Copy this file to .env.db and customize it if you want to run the database
# container with its own dedicated environment file.

DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
