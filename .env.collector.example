# Collector service environment variables.
# Copy this file to .env.collector and customize it if you want to run the
# collector container with its own dedicated environment file.

DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
